@model CoffeeShop.Models.Users

@{
    Layout = null;
}
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Login To Account</title>
</head>
<body>
    @Html.Partial("NavigationBar")
    <center>
        @using (Html.BeginForm("SubmitVipLogin", "Home", FormMethod.Post))
        {
            <table cellpadding="0" cellspacing="5">
                <tr>
                    <th colspan="2" align="center">Person Details</th>
                </tr>
                <tr>
                    <td>VIP Card Number: </td>
                    <td>
                        @Html.TextBoxFor(m => m.VIPNumber)
                    </td>
                </tr>
                <tr>
                    <td></td>
                    <td><input type="submit" value="Login" /></td>
                </tr>
            </table>
            if (@ViewBag.Message != null)
            {
                <div id="popupdiv" style="border: 1px solid red;">
                    @ViewBag.Message
                </div>
            }
        }
    </center>
</body>
</html>
